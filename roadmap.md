# 打磚塊 roadmap

> 現況基準：**2026-09-26 / v2.6.3 / sw v28**，兩個線上副本都要推到 v28。
> 這份只管「能力面」（要做什麼、為什麼不做）。玩法細節看 `README.md`，維護地雷看 `CLAUDE.md`。

---

## ✅ 已完成（別重做）

### 2026-09-26 這一輪(第三次收工)

| 項目 | 版本 | 一句話 |
| --- | --- | --- |
| **修正立體渲染鏡頭取景留白** | v2.6.3 | 使用者截圖問「線與擋板有到最底嗎」。上一輪把 `paddle.y` 的邏輯座標移到最底之後，鏡頭投影到螢幕上仍留一大截空白——直向手機球場窄長，鏡頭被「左右不能裁到磚牆」撐得比縱深需要的還遠，`updateProjectionMatrix()` 的對稱視角把多出來的配額硬分給板子那一端。改成不對稱視錐(off-axis frustum)：左右維持原本對稱公式，上/下拆開分別對準板子端與磚牆端實際角度，鏡頭距離不變。新增 `paddleScreenFrac` 驗收欄位並證明對舊版真的會抓到 |

### 2026-09-26 這一輪(第二次收工)

| 項目 | 版本 | 一句話 |
| --- | --- | --- |
| **修正立體渲染的寶物對齊** | v2.6.2 | 使用者實機回報「吃不到寶物」。跟危險線同一顆坑：道具原本畫在 2D 疊層，板子是透視投影的 3D 物件，兩者對不齊；碰撞判定本身沒壞，是畫面對不準。道具改成 `render3d.js` 的 `THREE.Sprite`（永遠面向鏡頭），貼圖用 Canvas 現畫、按「顏色+字樣」快取 |
| 直向板子/危險線貼齊底邊 | v2.6.2 | 使用者兩輪都要求「移到底」，`getPaddleBottomGap()` 改成跟橫向共用固定像素常數(28px)，不再依畫布高度算百分比；`test/patterns.mjs` 的拇指區安全網跟著拿掉(使用者知情決定，不是漏寫) |

### 2026-09-26 這一輪(第一次收工)

| 項目 | 版本 | 一句話 |
| --- | --- | --- |
| **修正立體渲染的危險線對齊** | v2.6.1 | 使用者實機回報「立體模式下線跑到板子下面去了」。真因是危險線原本畫在 2D 疊層(固定螢幕百分比)，板子是透視投影的 3D 物件，同一個 canvasY 兩套映射對不上。改成危險線也是 `render3d.js` 的 3D 物件，跟板子吃同一顆鏡頭 |
| 板子/危險線往下靠近底邊一些 | v2.6.1 | 直向手機的「拇指區」從畫布高度 8.5% 收到 6.5%，`test/patterns.mjs` 的「至少留 6% 拇指區」安全網仍過(這條在同一天第二輪被使用者要求更進一步、整個拿掉，見上方) |

### 2026-09-25 這一輪

| 項目 | 版本 | 一句話 |
| --- | --- | --- |
| **立體渲染（3D）模式** | v2.6.0 | 使用者問「能做 3D 版嗎」「都要另開一支嗎」「舊網站能 2D/2.5D 切換嗎」，拍板做成同一支 repo 裡的開關，不另開站。物理/存檔/分享碼完全不動，只換渲染層（`render3d.js` + 動態載入的 `vendor/three.module.js`）。設計決定與取捨寫在 `CLAUDE.md`「立體渲染（3D）模式的設計決定」段 |
| 新測試 `test/verify-browser-3d.mjs` | v2.6.0 | 切換／resize／偏好持久化，以及刻意模擬 render3d.js 載入失敗要靜默退回 2D 的迴歸 |

### 2026-09-17 這一輪

| 項目 | 版本 | 一句話 |
| --- | --- | --- |
| **板子長度五段可選** | v2.5.0 | 極短 0.7／短 0.85／標準／長 1.25／超長 1.6，倍率乘在難度板寬上；直向標準板 69px、超長 111px、極短 48px（邏輯像素，畫布寬 560） |
| 板長在遊戲中就能改 | v2.5.0 | 走 rescaleWorld() 同一條路，場上的板子當場變長變短，吃到的加寬寶物與卡關輔助一起等比例換算 |
| 上下限跟著倍率走 | v2.5.0 | 舊的寫死 clamp(…,100,260) 會把「極短」悄悄夾回去、讓「超長」的加寬寶物變成沒作用（兩條都由 test/patterns.mjs 守著） |
| 板長進紀錄 | v2.5.0 | 排行榜列與每日挑戰圖卡都標非預設板長；舊紀錄沒有這個欄位時不倒填做卡當下的設定 |
| 遊戲中那顆鈕改名「設定」 | v2.5.0 | 原本叫「音效」，板長放進去之後名字對不上內容（旁邊那顆「設定」是回首頁，改叫「首頁」） |
| 影子板上下限跟著場地縮 | v2.5.0 | 雙板寶物寫死的 clamp(…,72,150) 在直向會讓鏡像板比本體還寬（既有隱患，順手修） |

### 2026-09-16 這一輪

| 項目 | 版本 | 一句話 |
| --- | --- | --- |
| **手機直向遊玩** | v2.4.0 | 同一份程式、兩組版面常數（短邊固定 560）。磚牆、板寬、球速、下壓間隔、拇指區都依直向重算；不再強制轉橫向 |
| 畫面方向設定 | v2.4.0 | 「自動」（預設）或「固定橫向」；選固定橫向就是舊行為，蓋版提示仍在 |
| 轉向不會壞掉整局 | v2.4.0 | `rescaleWorld()` 把磚塊（照 row/col 重排）、球、板子、寶物、落石搬到新座標系，下壓進度靠 `state.descendRows` 帶過去 |
| 續玩存檔帶版面 | v2.4.0 | `RUN_SAVE_VERSION` 3→4，存 `cw/ch/pt/pb`；直向存的檔在橫向接得回來（順手修掉「橫向不同寬度之間續玩座標會跑掉」的舊隱患） |
| manifest 解除鎖定 | v2.4.0 | `orientation` `landscape`→`any`；鎖著的話裝成 App 的人連轉都轉不了 |
| 首頁橫向黏底開始鈕 | v2.4.0 | 0916 體檢：橫向 844×390 的可見範圍內一顆可點元素都沒有，要盲捲兩個半畫面 |
| 觸控目標補到 44px | v2.4.0 | 音量拉桿（原 13~16px）、核取方塊（原 13px）、下拉選單 |
| 每日挑戰分享圖卡 | v2.3.0 | 純 canvas 畫 1080×1440 PNG（日期／題號／分數／評級／四格數字／那一關的磚塊圖案縮圖），可分享、下載、複製文字 |
| 分享內容改成「今天」的成績 | v2.3.0 | 舊版印歷來每日最高分卻配今天的日期＝說謊；改存 `records.dailyToday`，跨日自動作廢 |
| 結算畫面「成績圖卡」鈕 | v2.3.0 | 每日挑戰結算才出現（一般關卡沒有日期與題號，給了也沒有分享價值） |
| `test/sharecard.mjs` | v2.3.0 | 假 ctx 驗「畫面上的字」33 項：漏 undefined／NaN、沒玩過卻印 0 分、檔名、文字與圖卡是否一致 |
| `test/verify-browser.mjs` | v2.3.0 | 真瀏覽器驗收進 repo（補舊 roadmap #4 的一半）：面板打得開、卡片不是空白、四顆鈕 ≥44px 且沒被角落元素偷走點擊 |
| ⛶ 手動全螢幕鈕 | v2.3.0 | 0916 手機體檢紅燈：原本只有「按開始時自動 requestFullscreen」，iOS Safari 與 App 內建瀏覽器擋掉就沒有第二次機會。只在瀏覽器真的支援時顯示 |
| manifest 鎖橫向 | v2.3.0 | 加 `"orientation": "landscape"`，裝成 APP 的路徑不再只依賴 runtime 的 `screen.orientation.lock` |
| 遊戲中 HUD 鈕加大 | v2.3.0 | 實測直向 40px、橫向只有 34px；「射擊」是快節奏操作鈕，補到 44px |

### 2026-09-15 這一輪

| 項目 | 版本 | 一句話 |
| --- | --- | --- |
| 關卡圖案佈局 | v1.6.0 | 10 張手設計 ASCII 圖案依關卡輪替，取代原本「每關都是 6×10 滿版矩形」 |
| 完賽打點 `-done` | v1.6.0 | 三層 beacon 補齊，統計不再只有「開啟次數」 |
| 磚塊顏色改主題色調暗 | v1.6.0 | 原本 hp≥3 寫死紅色，第 5 關起四個主題全失效 |
| 版本資訊帶日期 | v1.6.1 | `CHANGELOG` 改 `{ date, items }`，日期用 `git log -S` 回溯出來的真值 |
| 卡關輔助 | v1.7.0 | 同一關失誤兩次自動加寬板子，過關收回，設定可關 |
| 打擊感（震屏 + 頓幀） | v1.7.0 | 含 `prefers-reduced-motion` 支援 |
| 本機排行榜 Top 10 | v1.8.0 | 含舊 `bestScore` 遷移、「再多 N 分進榜」 |
| Boss 反擊 | v1.9.0 | 落石 + 0.85 秒紅色預告線，護盾可擋 |
| 磚塊定時下壓 | v2.0.0 | 依難度分級（休閒關閉），紅色危險線，撞線扣命並推回 |
| 續玩存檔 | v2.1.0 | 存整個場面，含 seeded RNG 位置 |
| 關卡編輯器 + 分享碼 | v2.2.0 | 10×3~8 格子，最長 43 字的分享碼，單字錯誤 100% 攔截 |

### 更早

PWA 安裝與更新提示、經典／每日挑戰、三難度、四主題、16 種寶物、成就、
特殊磚（爆破／鋼鐵／移動／分裂／加速／反彈）、Boss 大型磚、
直向／橫向兩套版面與手機相對拖曳、全螢幕、版號徽章、匿名遊玩計數。

---

## 🔜 待做（按 CP 值 × 開發時間排序）

| # | 項目 | ⏱ | ★ | 為什麼值得做 |
| --- | --- | --- | --- | --- |
| 1 | **雙人同機對戰** | ~4h | ★★ | 上下分屏各一塊板，平板上兩個孩子一起玩。要改動整個輸入與勝負架構 |
| 2 | **拆 `game.js`** | ~3h | ★★ | 已經 4800 行單檔。建議拆 `state / render / audio / storage / pwa / editor / sharecard` |
| 3 | **補齊 `test/verify-browser.mjs` 的關卡流程** | ~40m | ★★ | v2.4.0 已補進「開球 → 打磚 → 轉向 → 存檔」；**還沒驗到的是掉命、過關、Game Over 與續玩後真的接得上** |
| 4 | **返回作品集按鈕** | ~30m | ★ | 全艦隊有 `back-to-lobby-button` 慣例，這站沒接 |
| 5 | **關卡編輯器存本機草稿** | ~40m | ★ | 現在關掉分頁畫的圖就沒了，只能靠自己先複製分享碼 |
| 6 | **背景音樂換成預烤音檔** | ~2h | ★ | 現在是 8~15 音的方波循環，聽三分鐘會想關掉。可參考 skill `procedural-bgm` 或 `baked-voice-commentary` 的烤製流程 |

---

## 📋 待使用者拍板（別動工）

### 🧱 耶利哥城牆主題關卡包（約書亞記 6 章）

打磚塊 = 把城牆打倒，是**天然到不行**的聖經題材：7 關對應繞城七天，第 7 關城牆一次全塌。
工程量不大（一個主題 + 一組圖案佈局 + 幾句經文），但它會**改變這個站的定位**——
從「純街機」變成「可以在主日學用的教材」，並且要掛進聖經遊戲大廳。

⚠ 這是定位決策，不是技術決策，**使用者尚未拍板，不要自行開工**。
（2026-09-15 提出，使用者當日未回覆。）

---

## 🚫 刻意不做（連同理由，免得下次又被重新提案）

| 項目 | 為什麼不做 |
| --- | --- |
| **跨裝置排行榜 / 後端** | `localStorage` 只在本機這件事是刻意的。要跨裝置就得接後端與帳號，而這是一個零個資的小遊戲——不值得為了排行榜引進帳號系統。真要做請先解決「資料要不要離開裝置」這個前提 |
| **自訂關卡進排行榜** | 每張自訂圖難度天差地遠，混在一起比分數沒有意義。已在程式裡擋掉並寫進 `CLAUDE.md` |
| **npm / 框架 / 建置流程** | 純靜態是這個專案的核心資產：零依賴、`python -m http.server` 就能跑、部署沒有任何脆弱環節 |
| **MCP** | 這個遊戲沒有「要讓 AI 取用的外部資料源」——統計走既有的 `hfpc-play-stats` Worker，紀錄刻意只住使用者裝置。加 MCP 只是多一層。什麼時候才該做：如果哪天要讓 AI 直接查玩家紀錄，得先解決「資料要不要離開裝置」 |
| **搬離現有平台** | 兩個線上副本都在跑，CLAUDE.md 有雙推流程。搬家只會製造新的漂移 |
